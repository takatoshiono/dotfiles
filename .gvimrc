" for compatibility with kaoriya
set imdisable
AcpDisable

" window size
set lines=50
set columns=200

" fullscreen
"if has("gui_running")
"    set fuoptions=maxvert,maxhorz
"    au GUIEnter * set fullscreen
"endif

" color
colorscheme desert
